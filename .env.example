# Timezone
TZ=Europe/Berlin

# n8n
N8N_ENCRYPTION_KEY=replace_with_64_hex_chars
N8N_BASIC_AUTH_USER=admin
N8N_BASIC_AUTH_PASSWORD=replace_with_strong_password

# Postgres (n8n)
POSTGRES_DB=n8n
POSTGRES_USER=n8n
POSTGRES_PASSWORD=replace_with_strong_password

# Redis
REDIS_PASSWORD=replace_with_strong_password

# Local model (Ollama)
OLLAMA_MODEL=qwen3.5:27b

# Obsidian Local REST API
# Plugin default secure port is 27124 (self-signed TLS)
OBSIDIAN_REST_URL=https://host.docker.internal:27124
OBSIDIAN_REST_API_KEY=replace_with_obsidian_rest_api_key
OBSIDIAN_NOTES_DIR=21_Marketing/Social-Media/Beitraege
OBSIDIAN_ALLOW_INSECURE_TLS=true

# Optional Google Sheets logging (keep empty for fully local)
GOOGLE_SHEETS_LOGGING_ENABLED=false
